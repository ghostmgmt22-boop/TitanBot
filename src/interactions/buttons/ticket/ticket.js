import createTicketHandler, {
  closeTicketHandler,
  claimTicketHandler,
  priorityMenuHandler,
  pinTicketHandler,
  unclaimTicketHandler,
  reopenTicketHandler,
  deleteTicketHandler,
} from '../../../handlers/ticketButtons.js'

export default [
  createTicketHandler,
  closeTicketHandler,
  claimTicketHandler,
  priorityMenuHandler,
  pinTicketHandler,
  unclaimTicketHandler,
  reopenTicketHandler,
  deleteTicketHandler,
]
